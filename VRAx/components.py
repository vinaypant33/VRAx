from setuptools import setup

setup(
    name="vrax",
    version="0.1",
    py_modules=["window", "event", "components", "render"],
    install_requires=[],
    classifiers=[
        "Programming Language :: Python :: 3",
        "License :: OSI Approved :: MIT License",
    ],
    python_requires='>=3.6',
)
